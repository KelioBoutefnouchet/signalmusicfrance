(()=>{const list=document.querySelector('.shop-list');fetch('data/shop.json').then(r=>r.ok?r.json():Promise.reject()).then(items=>{list.innerHTML=items.map(item=>`<article class="shop-item"><header class="shop-meta"><strong>${item.categorie}</strong><span>${item.artiste}</span><span>${item.date}</span></header><p class="shop-description">${item.description}</p>${item.achat?`<a class="buy-button" href="${item.achat}" target="_blank" rel="noopener noreferrer">Acheter <span aria-hidden="true">🛒</span></a>`:'<span class="buy-button is-disabled" aria-disabled="true" title="Lien d’achat à renseigner">Acheter <span aria-hidden="true">🛒</span></span>'}<div class="shop-images">${item.images.map((image,i)=>`<img src="${image}" alt="${item.categorie} ${item.artiste} — vue ${i+1}" loading="lazy" decoding="async">`).join('')}</div></article>`).join('')}).catch(()=>list.innerHTML='<p class="load-error">Boutique indisponible.</p>')})();
